<h1><%= @user.username %></h1>

<form action="<%= subs_url %>" method="GET">
    <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
    <input type="submit" value="See them subs!">
</form>